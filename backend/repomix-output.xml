This file is a merged representation of the entire codebase, combined into a single document by Repomix.

<file_summary>
This section contains a summary of this file.

<purpose>
This file contains a packed representation of the entire repository's contents.
It is designed to be easily consumable by AI systems for analysis, code review,
or other automated processes.
</purpose>

<file_format>
The content is organized as follows:
1. This summary section
2. Repository information
3. Directory structure
4. Repository files (if enabled)
5. Multiple file entries, each consisting of:
  - File path as an attribute
  - Full contents of the file
</file_format>

<usage_guidelines>
- This file should be treated as read-only. Any changes should be made to the
  original repository files, not this packed version.
- When processing this file, use the file path to distinguish
  between different files in the repository.
- Be aware that this file may contain sensitive information. Handle it with
  the same level of security as you would the original repository.
</usage_guidelines>

<notes>
- Some files may have been excluded based on .gitignore rules and Repomix's configuration
- Binary files are not included in this packed representation. Please refer to the Repository Structure section for a complete list of file paths, including binary files
- Files matching patterns in .gitignore are excluded
- Files matching default ignore patterns are excluded
- Files are sorted by Git change count (files with more changes are at the bottom)
</notes>

</file_summary>

<directory_structure>
asterisk/
  scripts/
    call_handler.py
  extensions.conf
  manager.conf
  sip.conf
backend/
  ramais/
    migrations/
      __init__.py
      0001_initial.py
    __init__.py
    admin.py
    apps.py
    models.py
    serializers.py
    tests.py
    views.py
  voip/
    __init__.py
    asgi.py
    settings.py
    urls.py
    wsgi.py
  db.sqlite3
  manage.py
.gitignore
.python-version
main.py
pyproject.toml
README.md
</directory_structure>

<files>
This section contains the contents of the repository's files.

<file path="asterisk/scripts/call_handler.py">
import requests
from asterisk.agi import AGI

agi = AGI()

caller = agi.env.get('agi_callerid')

response = requests.get(f'http://127.0.0.1:8000/api/extensions/')

agi.verbose(f"Caller ID: {caller}")
agi.verbose(f"Response from API: {response.text}")
</file>

<file path="asterisk/extensions.conf">
[internal]
exten => 1001,1,Dial(SIP/1001)
exten => 1002,1,Dial(SIP/1002)
</file>

<file path="asterisk/manager.conf">
[admin]
secret = senha123
read = all
write = all
</file>

<file path="asterisk/sip.conf">
[1001]
type=friend
host=dynamic
secret=1234
context=internal

[1002]
type=friend
host=dynamic
secret=1234
context=internal
</file>

<file path="backend/ramais/migrations/__init__.py">

</file>

<file path="backend/ramais/migrations/0001_initial.py">
# Generated by Django 6.0.3 on 2026-03-17 22:36

from django.db import migrations, models


class Migration(migrations.Migration):

    initial = True

    dependencies = [
    ]

    operations = [
        migrations.CreateModel(
            name='Extension',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('number', models.CharField(max_length=10)),
                ('password', models.CharField(max_length=50)),
            ],
        ),
    ]
</file>

<file path="backend/ramais/__init__.py">

</file>

<file path="backend/ramais/admin.py">
from django.contrib import admin

# Register your models here.
</file>

<file path="backend/ramais/apps.py">
from django.apps import AppConfig


class RamaisConfig(AppConfig):
    name = 'ramais'
</file>

<file path="backend/ramais/models.py">
from django.db import models

class Extension(models.Model):
    number = models.CharField(max_length=10)
    password = models.CharField(max_length=50)

    def __str__(self):
        return self.number
</file>

<file path="backend/ramais/serializers.py">
from rest_framework import serializers
from .models import Extension

class ExtensionSerializer(serializers.ModelSerializer):
    class Meta:
        model = Extension
        fields = '__all__'
</file>

<file path="backend/ramais/tests.py">
from django.test import TestCase

# Create your tests here.
</file>

<file path="backend/ramais/views.py">
from rest_framework import viewsets, status
from rest_framework.response import Response
from rest_framework.views import APIView
from asterisk.ami import AMIClient, SimpleAction
from .models import Extension
from .serializers import ExtensionSerializer

class ExtensionViewSet(viewsets.ModelViewSet):
    queryset = Extension.objects.all()
    serializer_class = ExtensionSerializer

class MakeCallView(APIView):
    def post(self, request, *args, **kwargs):
        caller = request.data.get('from')
        callee = request.data.get('to')

        if not caller or not callee:
            return Response(
                {'error': 'Both "from" and "to" fields are required.'}, 
                status=status.HTTP_400_BAD_REQUEST
                )
        
        try: 
            client = AMIClient(address='127.0.0.1', port=5038)
            client.login(username='admin', secret='password123')

            action = SimpleAction(
                'Originate',
                Channel=f'PJSIP/{callee}',
                Exten=caller,
                Context='from-internal',      
                Priority=1,
                CallerID=f'API Call <{callee}>'
            )

            client.send_action(action)
            client.logoff()

            return Response({'message': f'Call from {caller} to {callee} initiated successfully.'})
        except Exception as e:
            return Response({'error': str(e)}, status=status.HTTP_500_INTERNAL_SERVER_ERROR)
</file>

<file path="backend/voip/__init__.py">

</file>

<file path="backend/voip/asgi.py">
"""
ASGI config for voip project.

It exposes the ASGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/asgi/
"""

import os

from django.core.asgi import get_asgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'voip.settings')

application = get_asgi_application()
</file>

<file path="backend/voip/settings.py">
"""
Django settings for voip project.

Generated by 'django-admin startproject' using Django 6.0.3.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/6.0/ref/settings/
"""

from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/6.0/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-9!douo)na8fr(mud815q8+p2@f(atijuh%$y2do0i8ew5@5l=b'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'ramais',
    'rest_framework',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'voip.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'voip.wsgi.application'


# Database
# https://docs.djangoproject.com/en/6.0/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': BASE_DIR / 'db.sqlite3',
    }
}


# Password validation
# https://docs.djangoproject.com/en/6.0/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/6.0/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/6.0/howto/static-files/

STATIC_URL = 'static/'
</file>

<file path="backend/voip/urls.py">
from django.contrib import admin
from django.urls import path, include
from rest_framework.routers import DefaultRouter
from ramais.views import ExtensionViewSet, MakeCallView

router = DefaultRouter()
router.register(r'extensions', ExtensionViewSet)
router.register(r'make-call', MakeCallView, basename='make-call')

urlpatterns = [
    path('admin/', admin.site.urls),
    path("api-auth/", include("rest_framework.urls")),
    # path('api/', include(router.urls)),
    path('api/make-call/', MakeCallView.as_view(), name='make-call'),
]
</file>

<file path="backend/voip/wsgi.py">
"""
WSGI config for voip project.

It exposes the WSGI callable as a module-level variable named ``application``.

For more information on this file, see
https://docs.djangoproject.com/en/6.0/howto/deployment/wsgi/
"""

import os

from django.core.wsgi import get_wsgi_application

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'voip.settings')

application = get_wsgi_application()
</file>

<file path="backend/manage.py">
#!/usr/bin/env python
"""Django's command-line utility for administrative tasks."""
import os
import sys


def main():
    """Run administrative tasks."""
    os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'voip.settings')
    try:
        from django.core.management import execute_from_command_line
    except ImportError as exc:
        raise ImportError(
            "Couldn't import Django. Are you sure it's installed and "
            "available on your PYTHONPATH environment variable? Did you "
            "forget to activate a virtual environment?"
        ) from exc
    execute_from_command_line(sys.argv)


if __name__ == '__main__':
    main()
</file>

<file path=".gitignore">
# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv
</file>

<file path=".python-version">
3.12
</file>

<file path="main.py">
def main():
    print("Hello from sistema-voip!")


if __name__ == "__main__":
    main()
</file>

<file path="pyproject.toml">
[project]
name = "sistema-voip"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "djangorestframework>=3.16.1",
]
</file>

<file path="README.md">

</file>

</files>
